package me.xxgradzix.gradzixcombatsystem.listeners.enchants;

import me.xxgradzix.gradzixcombatsystem.managers.EnchantManager.EnchantManager;
import org.bukkit.event.EventHandler;
import org.bukkit.event.EventPriority;
import org.bukkit.event.Listener;
import org.bukkit.event.entity.EntityShootBowEvent;
import org.bukkit.inventory.ItemStack;

public class MultiShot implements Listener {

//    @EventHandler(priority = EventPriority.LOWEST)
//    public void onMultiShot(EntityShootBowEvent event) {
//
//        ItemStack bow = event.getBow();
//
//        if(bow == null) return;
//
//        int enchantLevel = EnchantManager.getEnchantLevel(bow, EnchantManager.Enchant.MULTI_SHOT);
//
//        if(enchantLevel == 0) return;
//
//
////        if(enchantLevel == 1) {
//
////        } else if(enchantLevel == 2) {
////            event.setProjectile(event.getProjectile());
////        } else if(enchantLevel == 3) {
////            event.setProjectile(event.getProjectile());
////        }
//
//
//
//    }

}
